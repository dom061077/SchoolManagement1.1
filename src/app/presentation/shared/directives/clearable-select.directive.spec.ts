import { ClearableSelectDirective } from './clearable-select.directive';

describe('ClearableSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearableSelectDirective();
    expect(directive).toBeTruthy();
  });
});
